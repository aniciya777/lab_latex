\begin{frame}[fragile]{Работа с пакетом listings}
    \hypersetup{pdfpagetransition={Box /D 2 /M /O}}
    
    Результат:
    
    \begin{lstlisting}
                max = A[0];
                i_max = 0;
                for(i = 1; i < n; i++)
                    if (A[i] > max) 
                    {
                        max = A[i];
                        i_max = i;
                    }
    \end{lstlisting}
    
\end{frame}
